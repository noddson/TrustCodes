{
  "name": "trust-codes",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "generate:version": "node generate-version.mjs",
    "start": "npm run generate:version && python3 -m http.server 4173",
    "test": "node --test",
    "validate:version": "node validate-version.mjs",
    "check": "npm test && npm run validate:version"
  },
  "devDependencies": {
    "jsqr": "^1.4.0",
    "qrcode-generator": "^2.0.4"
  }
}
